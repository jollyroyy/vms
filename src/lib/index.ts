// Placeholder so `tsc --noEmit` has an input before the first feature slice.
// Modules land here as the loop turns tests/pending.list suites green:
// refNumber, visitLifecycle, gatePassStatus, rgpDueDate, blacklist, photo.
export {};
