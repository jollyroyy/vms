# Test suites derived from goal.md §2.2, awaiting activation by the loop.
# RED STEP (goal.md §3.3): delete ONE line to activate that suite → it fails →
# implement to green → commit (pre-commit hook enforces green).
# Never delete a test file to "fix" red — only implementation makes green.
